{
  "name": "xml-replace-action",
  "version": "2.0.0",
  "description": "Simple github action to replace a value in a xml config file",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@xmldom/xmldom": "^0.8.10",
    "xpath": "^0.0.33"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
